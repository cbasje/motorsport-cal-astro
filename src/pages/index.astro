---
import WeekendList from "src/components/WeekendList.svelte";
import MainLayout from "../layouts/main.astro";
---

<MainLayout>
    <div class="container">
        <WeekendList client:idle />
    </div>
</MainLayout>

<style lang="scss">
    .container {
        display: flex;
        flex-direction: column;
        gap: var(--size-3);
        max-inline-size: var(--size-content-2);
        margin: 0 auto;
        padding: 0 var(--size-4);
    }
</style>
